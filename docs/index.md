# Ticket Triage LoRA

This project fine-tunes a small instruction model using LoRA to classify IT tickets into:

- category
- priority
- route_to
- next_action

Output is strict JSON to support downstream automation (routing, SLA rules, dashboards).
